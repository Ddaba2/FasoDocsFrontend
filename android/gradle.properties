org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
# Reduce warning verbosity
org.gradle.warning.mode=summary
# Suppress deprecation warnings from third-party plugins
org.gradle.warning.on.deprecated=false
# Suppress specific Java version warnings
kotlin.jvm.target.validation.mode=warning
# Améliorer la stabilité du débogage Flutter
org.gradle.daemon=true
org.gradle.parallel=false
org.gradle.configureondemand=false
# Kotlin daemon settings to improve stability and prevent connection failures
kotlin.daemon.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -Dfile.encoding=UTF-8
kotlin.daemon.useFallbackStrategy=true
kotlin.incremental=false
# Additional Gradle stability settings
org.gradle.caching=true
org.gradle.workers.max=2
# Force Kotlin daemon to restart on each build
kotlin.daemon.keepAlive=false